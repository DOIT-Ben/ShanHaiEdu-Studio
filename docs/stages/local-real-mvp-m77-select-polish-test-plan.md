# Local Real MVP M77 下拉框统一优化测试计划

更新时间：2026-07-11

1. 检查 `src` 中不再存在原生 `<select>`。
2. 检查成员新增、成员权限更新和账号角色选择均使用统一 Select。
3. 检查触发器的展开态、悬浮态、焦点态及选项的 highlighted/checked 状态。
4. 运行成员管理回归测试、TypeScript、全量测试、生产构建与 `git diff --check`。
5. 浏览器检查协作成员弹窗中展开、键盘选择、保存及窄屏边界。
