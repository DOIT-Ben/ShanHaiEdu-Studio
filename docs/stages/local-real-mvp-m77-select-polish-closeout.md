# Local Real MVP M77 下拉框统一优化收尾

更新时间：2026-07-12

## 结论

状态：`done`。源码、自动化和真实项目 owner 写路径均通过。成员新增和已有成员权限修改均使用统一 Select，完成展开、键盘选择、保存、刷新恢复和 390px 弹层边界验收。

## 证据

- `src` 无原生 `<select>`；成员新增、成员权限和账号角色均由统一 Select 承载。
- M77/M78 合同覆盖 open、highlighted、checked、disabled、Popper、滚动和窄屏边界。
- 1366×768 真实浏览器可打开协作成员弹窗并正确显示只读权限边界。
- 2026-07-12 使用独立 password-auth 临时数据库创建真实 owner 项目：新增成员时键盘将“可查看”改为“可编辑”，POST 返回 201；已有成员键盘改回“可查看”，PATCH 返回 200；刷新后仍显示“可查看”。
- 390×844 下对话框、触发器、选项列表、勾选和移除按钮均在视口内，无横向溢出或遮挡。证据：`.playwright-cli\page-2026-07-11T18-01-49-316Z.png`。
- 新鲜全量证据：Node 259/259、Vitest 482/482、生产构建和 `git diff --check` 通过。
- 全量测试和生产构建通过。

## 未关闭门禁

M77 本地验收门已经关闭。目标服务器与真实用户邀请仍按 V1 发布计划单独验证。

## 回退

不修改 `v1`；回退仅针对后续文档提交。
