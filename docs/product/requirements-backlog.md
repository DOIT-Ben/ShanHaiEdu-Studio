# ShanHaiEdu 未完成需求总账

更新时间：2026-07-19

本文件只记录未完成、延期或未来需求。

## 当前P0

| ID | 未完成项 | 完成条件 | 当前边界 |
|---|---|---|---|
| P0-DR | 产品优先深度重构（活动） | 核心合同漏洞关闭；旧控制面与deterministic生产路径删除；复杂度、源码字符串合同、Lint和构建追踪债务清零；最终HEAD全量验证 | 不调用真实Provider，不创建V1-9，不生成媒体或整包 |
| P0-05A | 真实Provider连续性（暂停） | 深度重构关闭后重新冻结候选；另获channel、model、费用、次数和签名key授权；四场景连续3组通过receipt校验 | 当前0调用；旧阶段计划已归档，不能直接恢复 |
| P0-05B | 唯一V1-9真实产品链路 | P0-05A Go后创建新run并通过桌面全链路、文件验真与外部产物验收 | 不恢复旧run、旧manifest或旧固定流程 |
| P0-06 | V1签收与发布门 | V1-9通过后完成教师签收、候选环境、恢复、原子切流和发布后验证 | 部署与生产写入另取当次授权 |

## 当前阶段排除项

- 不增加第二个Main Agent、Planner、Director、Critic或Runner控制面。
- 不用mock、placeholder、deterministic fallback或degraded结果冒充成功。
- 不调用图片、视频、PPTX、ZIP或整包Provider。
- 不运行390px真实黑盒，不部署，不执行教师签收。
- 不通过提高阈值、扩大排除目录或删除有效测试清零债务。

## 已接受未来需求

其他产品、架构、UI和发布工作统一从`..\roadmap\README.md`进入。Roadmap不自动取得执行权。

## 状态变更规则

新需求先记录目标、验收层和优先级；进入当前阶段后由唯一plan/test-plan控制；完成后从本表移除。Git历史负责保存流水账，活动文档不维护done列表。
