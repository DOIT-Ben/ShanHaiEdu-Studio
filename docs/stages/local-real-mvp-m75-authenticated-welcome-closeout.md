# Local Real MVP M75 登录后欢迎首页收尾

更新时间：2026-07-11

## 结论

状态：`done`。登录/刷新后不自动进入历史项目，教师主动选择或新建后才进入工作台。

## 证据

- 自动化覆盖初始化不读 snapshot/localStorage、空 active 状态、最多 4 个最近项目、生命周期视图和 reduced motion。
- 真实浏览器登录后先显示“今天想准备哪一节课？”；账号甲重新登录后显示最近项目但不自动进入。
- 点击最近项目后恢复消息、成果 rail 和三栏工作台；账号乙无项目时保持欢迎空态。
- 1366×768 与 390px 均完成真实页面检查；工程门禁通过。

## 回退

无数据迁移；保留 `v1`，只回退后续文档提交即可。
