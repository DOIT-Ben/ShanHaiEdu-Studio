# V1 Agent 与交付质量资料迁入计划

更新时间：2026-07-12

## 1. 目标

将桌面上的 Agent 架构审计、交付工艺设计、PPT/视频工作流优化及其实验依据纳入 ShanHaiEdu 项目文档体系，形成后续 V1 实施可直接读取的统一入口。

## 2. 来源与目标

来源：

- `E:\desktop\ShanHaiEdu-Agent架构审计资料库-20260711`
- `E:\desktop\ShanHaiEdu-智能体与交付工艺架构设计-20260711`
- `E:\desktop\ShanHaiEdu-交付效果工作流优化-2026-07-11`

目标：

```text
docs\architecture\2026-07-11-v1-agent-delivery-quality\
```

目标目录下保留三个原始目录名，避免破坏它们之间已有的相对引用。

## 3. 边界

- 只迁入文档、合同、提示词、研究来源、实验脚本和脱敏实验证据。
- 不修改业务代码、数据库、Provider 配置和运行时。
- 迁入内容属于 V1 候选设计与证据包；产品需求仍以 `docs\product\current-requirements-baseline.md` 为最高口径。
- 本轮先复制并校验。桌面源目录作为回退副本保留，删除必须另行确认。

## 4. 风险与控制

| 风险 | 控制 |
|---|---|
| 跨目录链接失效 | 保留三个原始目录名，并运行本地链接检查 |
| 敏感数据进入仓库 | 迁入前扫描 JSON 敏感字段；不输出或记录密钥值 |
| 历史候选设计覆盖当前权威口径 | 新增统一 README，明确文档状态、权威顺序和适用边界 |
| 二进制实验资产造成不必要膨胀 | 保留约 11MB 的有限 A/B 实物证据，并在清单中记录数量、体积和哈希 |
| 迁移失败或内容不完整 | 比较来源/目标相对路径、文件数量、字节数和 SHA-256 |

## 5. 验收

1. 三个来源目录均在项目内有完整对应目录。
2. 来源与目标文件数量、相对路径、字节数和 SHA-256 一致。
3. 统一 README 和项目架构索引可定位全部资料。
4. Markdown 本地链接无新增断链；历史绝对路径明确标注为审计快照。
5. `git diff --check` 通过，工作树只包含本轮文档迁移范围。
