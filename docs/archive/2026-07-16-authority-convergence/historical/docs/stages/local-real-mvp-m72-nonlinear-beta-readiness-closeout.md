# Local Real MVP M72 非线性能力与团队内测收尾

更新时间：2026-07-12

## 结论

状态：`done`。反馈、评价、服务端隔离、CSRF、会话撤销、历史归属和按需视频脚本路径均已有真实证据。“只做视频脚本”在缺少硬前置时会点名导入创意主题和最小补充信息，不再返回空泛确认或进入无解循环，也不会自动扩张到 PPT 或最终视频。

## 验收证据

- 基线：annotated tag `v1` 指向 `fffdfb3b050782208bb6e288d3e324ba44a4c659`。
- 自动化：`npm test` 通过，Node 259/259、Vitest 482/482；新增能力级缺口说明测试，并保持对话控制、反馈、PlanGuard、双账号授权和 CSRF 合同通过。
- 真实浏览器：1366×768 和 390px 均可登录、创建项目、发送消息、打开成果抽屉和提交反馈；反馈 POST 返回 201。
- 双账号：账号乙读取账号甲项目 snapshot、messages、artifacts 和下载入口均返回 404；读取管理员反馈返回 403；缺少 CSRF 的项目写入返回 403。
- 会话撤销：管理员撤销账号乙会话返回 200，随后 `/api/auth/me` 显示未认证。
- 历史归属：指定历史账号现有 23 个项目、240 条消息、50 个产物，owner membership 不一致数为 0；数量包含 v1 验收前后的本地增量，只用于当前快照证明。
- 2026-07-12 真实浏览器：使用独立 password-auth 临时数据库和真实 owner，在 1366×768 与 390×844 输入“只做视频脚本”；后端队列完成后返回明确最小缺口，窄屏无溢出或遮挡。
- 浏览器证据：`.playwright-cli\page-2026-07-11T17-59-21-969Z.png`、`.playwright-cli\page-2026-07-11T17-59-35-400Z.png`。

## 未关闭门禁

- 真实 Provider、PPTX、图片、视频和最终包仍属于 V1 发布门，不影响 RQ-015 本地交互需求关闭。
- 旧 M67 E2E 仍查找已废弃标题“登录 ShanHaiEdu”，本次 2 个首测失败、其余未运行；产品手工验收替代通过，但旧脚本需要后续独立修订。

## 回退

保留 `v1` 不动；若本次行为出现回归，只回退 `capability-availability.ts` 的能力级缺口说明和对应测试，不回退其他 M72 已验证能力。
