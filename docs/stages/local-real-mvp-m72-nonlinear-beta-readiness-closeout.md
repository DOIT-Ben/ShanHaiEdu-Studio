# Local Real MVP M72 非线性能力与团队内测收尾

更新时间：2026-07-11

## 结论

状态：`implementation verified / acceptance gap`。反馈、评价、服务端隔离、CSRF、会话撤销和历史归属已有证据；真实浏览器中“只做视频脚本”仍返回“请先确认前置成果后再继续”，未完全满足按需直达口径，因此 RQ-015 暂不标记 `done`。

## 验收证据

- 基线：annotated tag `v1` 指向 `fffdfb3b050782208bb6e288d3e324ba44a4c659`。
- 自动化：`npm test` 单 worker 通过，Node 259/259、Vitest 481/481；对话控制、反馈分类、评价持久化、PlanGuard、双账号授权和 CSRF 合同均在通过集内。
- 真实浏览器：1366×768 和 390px 均可登录、创建项目、发送消息、打开成果抽屉和提交反馈；反馈 POST 返回 201。
- 双账号：账号乙读取账号甲项目 snapshot、messages、artifacts 和下载入口均返回 404；读取管理员反馈返回 403；缺少 CSRF 的项目写入返回 403。
- 会话撤销：管理员撤销账号乙会话返回 200，随后 `/api/auth/me` 显示未认证。
- 历史归属：指定历史账号现有 23 个项目、240 条消息、50 个产物，owner membership 不一致数为 0；数量包含 v1 验收前后的本地增量，只用于当前快照证明。

## 未关闭门禁

- 真实 Provider、PPTX、图片、视频和最终包未获授权，未执行。
- 按需视频脚本的真实浏览器路径仍有前置确认差距。
- 旧 M67 E2E 仍查找已废弃标题“登录 ShanHaiEdu”，本次 2 个首测失败、其余未运行；产品手工验收替代通过，但旧脚本需要后续独立修订。

## 回退

保留 `v1` 不动；若 M72 行为出现回归，以 `fffdfb3` 为产品代码回退点，只回退后续文档提交。
