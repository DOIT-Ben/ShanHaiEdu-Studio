# ShanHaiEdu `v1` 标签基线复核与收尾任务指令

更新时间：2026-07-11

审查对象：`E:\desktop\AI\11_Products\lab\ShanHaiEdu-Studio\main`

审查方式：只读代码、文档、Git 与本地自动化；未修改项目文件，未调用真实 Provider，未变更目标服务器。

## 1. 结论

旧收尾指令已经失效：M72-M78 已随 `fffdfb3b050782208bb6e288d3e324ba44a4c659` 提交到 `main`，与 `origin/main` 一致，工作树干净，并存在 annotated tag `v1`。

新的收尾任务不是继续提交功能，而是：

> 把 `v1` 从“已打标签的工程候选”封板为“证据完整、状态真实、可供主 Codex 接手 V1 交付主线的基线”。

V1 主线需要更新，但只更新阶段 0 和基线判断。执行安全、合同/质量、PPT Quality、视频 Full Intro、最终交付和真实教师验收仍然必须完成。

## 2. 当前已证实

| 项目 | 证据 |
|---|---|
| HEAD | `fffdfb3b050782208bb6e288d3e324ba44a4c659` |
| 远端关系 | `main` 与 `origin/main` ahead 0 / behind 0 |
| 工作树 | clean，tracked 0、untracked 0 |
| 标签 | annotated tag `v1`，指向 `fffdfb3` |
| 变更规模 | 相对旧基线 `02703d0`：130 files、3451 insertions、788 deletions |
| 完整测试 | `npm test` exit 0；Node 259/259，Vitest 67 files / 481 tests |
| 构建 | `npm run build` exit 0；Next.js 16.2.10 编译、TypeScript、13 个静态页面均完成 |
| diff | `git diff --check` exit 0 |
| Graphify | 报告更新时间 22:57，能检索到 AuthenticatedWelcome、InteractiveListRow、artifact capability 和 ConversationControlResolver 测试节点 |

## 3. 尚未封板的问题

### P1：标签名称超过当前发布证据

`v1` 标签和提交信息使用“完成”“优化完毕”，但权威主线仍明确记录真实 Provider smoke、M67/M69 目标服务器重启/回滚/备份恢复和真实教师端到端尚未关闭。

处置：不移动或删除标签；在 closeout 和主线状态中将其定义为 `engineering candidate`，直至发布门通过。

### P1：M72-M78 没有 closeout

七个阶段均有 plan/test-plan，但 `docs\stages\` 中 M72-M78 closeout 数量为 0。M73-M76 测试计划还明确写明浏览器由主代理执行、执行代理不声明通过。

处置：逐阶段补 closeout，自动化、浏览器、真实账号和未验证项分开记录。

### P1：需求状态与提交口径没有对账

RQ-015 到 RQ-021 在 backlog 中仍是 `accepted`，其中 M72 还包含双账号隔离、历史项目归属和真实团队内测审查。一次 commit 和标签不能自动把这些状态改为 `done`。

处置：逐条证据对账；缺证据的切片保持 accepted/partial，不做批量完成。

### P2：权威主线状态滞后

`current-mainline-status.md` 的最近阶段仍停在 M71A，未记录 M72-M78、`fffdfb3` 或 `v1`，下一阶段文字仍写“M71 视频结构化前置链路”。

处置：更新主线事实、保留真实上线门，并明确下一条业务主线是 V1 交付质量，而不是继续 UI 扩张。

### P2：一次提交跨越 130 个文件

提交同时包含数据库、认证、对话控制、反馈、Artifact UI、欢迎页、基础组件和 35+ 测试，回滚粒度较粗。

处置：不改写历史。closeout 中建立 M72-M78 文件/测试/回退映射，后续 V1 坚持共享底座单一 owner、PPT/视频独立 worktree。

## 4. V1 主线变化

| 原计划 | 最新判断 |
|---|---|
| 先收口 M72-M75 脏工作树 | 已完成提交；改为验证并封板 M72-M78 的 `v1` 标签 |
| HEAD `02703d0`、ahead 14、111 项工作树 | HEAD `fffdfb3`、ahead/behind 0、工作树 clean |
| ConversationControlResolver 尚在途 | 已进入代码，可作为 V1 有界控制入口复用；仍不等于 ReAct Harness |
| Artifact/UI 基线未稳定 | M73-M78 已形成能力导航、欢迎页和 UI primitives 候选基线，需浏览器封板 |
| 现实工期 14-21 天 | 封板后约 12-18 天；PPT/视频质量主链工作量基本不变 |

不变的 V1 核心：

1. actor fail-closed、ProjectExecutionLease、IntentEpoch/commit fence、Provider 幂等与恢复。
2. 可执行 Node Contract、Validator、Critic、QualityDecision 和 FinalDeliveryGate。
3. PPT Quality 的大纲、PageSpec、样张、正式资产、可编辑 PPTX、render 和页级返修。
4. 视频 Full Intro 的课程锚点、ShotSpec、参考资产实传、逐镜头 Job、FFmpeg 和镜头级返修。
5. 三个真实数学任务、真实 Provider、目标服务器恢复门和教师课堂演练。

## 5. 可直接发送的收尾任务指令

```text
你现在负责 ShanHaiEdu `v1` 标签的验证封板与正式交接，不再继续扩展新功能。

项目目录：
E:\desktop\AI\11_Products\lab\ShanHaiEdu-Studio\main

当前候选基线：
- main HEAD：fffdfb3b050782208bb6e288d3e324ba44a4c659
- origin/main：与本地一致
- annotated tag：v1
- 工作树：clean

目标：
把当前 `v1` 从“已提交并打标签的工程候选”收口为“证据完整、文档真实、下一位主 Codex 可以安全接手的基线”。不要把标签名称、提交信息或测试文件存在当作验收完成。

一、冻结范围

1. 停止新增 M79、视觉优化、组件抽离和无关重构。
2. 只允许修复本次验收发现的阻塞缺陷、补测试、补 closeout 和同步权威文档。
3. 不删除、不重写、不强制移动既有 `v1` 标签；不 force push。
4. 不改写已经推送的 130 文件提交历史。

二、逐阶段对账 M72-M78

分别对照每个 plan/test-plan，建立：需求 -> 实现文件 -> 测试 -> 浏览器证据 -> 遗留风险 -> 回退点。

- M72：反馈稳定性、消息评价、ConversationControlResolver、自然语言确认/改道、HumanGate 边界、双账号隔离和历史归属。
- M73：Artifact 能力分组、单项直开、多项抽屉、返回来源和窄屏可达性。
- M74：品牌认证页、注册门禁、字段合同、桌面/移动响应式和可信文案。
- M75：登录/刷新欢迎页、不自动进入旧项目、主动选择/创建项目和失败状态。
- M76：InteractiveListRow 三个批准接入点、菜单阻止冒泡、focus/disabled/窄屏状态。
- M77：成员权限与账号角色 Select 迁移，不改变枚举和保存回调。
- M78：Input、Textarea、MenuItem、Select 和浮层 tokens；保留 PromptComposer、隐藏 file input 等特殊边界。

每个阶段新建对应 closeout：
docs\stages\local-real-mvp-m72-...-closeout.md
...
docs\stages\local-real-mvp-m78-...-closeout.md

三、重新执行工程验证

依次运行并记录完整结果、计数和 exit code：

git status --short --branch
git rev-parse HEAD
git rev-list --left-right --count origin/main...main
npm test
npm run build
git diff --check

完成后检查是否残留属于本轮的 Vitest/Jest/Playwright worker；不要停止与本轮无关的既有 Playwright daemon。

四、真实浏览器验收

至少检查 1366x768 和 390px：

1. 登录页、注册门禁开/关、错误/loading/disabled。
2. 登录或刷新后停留欢迎页，不自动加载旧项目 snapshot。
3. 主动选择最近项目、创建项目成功/失败。
4. Artifact rail 最多 6 个能力入口；单项直开、多项/全部进入抽屉；详情返回原分组。
5. InteractiveListRow、Select、Input、Textarea、MenuItem 的 hover/focus/selected/disabled 和无横向溢出。
6. 反馈七类切换不跳动；赞、踩、取消和失败提示可访问。
7. “确认需求并生成大纲”“只做视频脚本”“改做分镜”等自然语言不会进入无解循环。
8. Provider、package、外部写入和高成本工具仍必须经过真实 HumanGate。

截图和报告必须遮挡账号、路径、token、项目隐私和 Provider 信息。

五、账号和权限验收

使用两个真实密码测试账号验证：

- 未共享项目、消息、产物、下载和反馈互不可见。
- 指定账号能看到受控绑定的历史项目，迁移前后数量一致。
- 停用、重置凭据、角色变化会撤销既有会话。
- 写操作保持 CSRF 门禁，响应和日志不回显密码或 session token。

六、同步权威文档

1. 更新 docs\product\requirements-backlog.md。
   - 只有证据完整的 RQ 才改为 done。
   - M72 中未完成的真实团队内测或账号归属切片必须保持 partial/accepted，并写清原因。
2. 更新 docs\mainlines\current-mainline-status.md。
   - 记录 M72-M78、fffdfb3 和 v1 标签。
   - 明确 v1 当前是 engineering candidate 还是 release accepted。
   - 保留真实 Provider smoke、M67/M69 目标服务器重启/回滚/备份恢复和教师 E2E 门禁。
七、生产与 Provider 边界

如果本任务没有明确授予真实 Provider 费用和生产服务器操作权限：
- 不调用付费 API。
- 不重启、部署或修改目标服务器。
- 将真实 Provider、共享卷重启、release 回滚、备份恢复标记为未关闭门禁。

如果权限已明确授予，则按现有 runbook 执行并保留脱敏证据；mock、deterministic、文件名或成功状态不得替代真实实物。

八、提交和交接

1. 形成一个新的非破坏性 closeout/docs 提交，不修改既有 v1 标签。
2. 提交信息遵守项目 AGENTS.md 中文格式。
3. 未经明确授权不 push、不部署、不发布。
4. 提交最终交接报告，包含：
   - 最终 SHA、分支、tag 指向、ahead/behind、工作树状态。
   - M72-M78 状态矩阵。
   - 测试/build/浏览器/账号验证证据。
   - 未关闭的 Provider、服务器、教师门禁。
   - 130 文件提交的阶段文件映射和独立回退建议。
   - 建议主 Codex 接手的准确起点。

验收标准：
- 七份 closeout 存在且事实与证据一致。
- npm test、npm run build、git diff --check 为新鲜 exit 0。
- 桌面和 390px 浏览器关键流程有真实证据。
- requirements backlog 与 current mainline 不再停留在 M71A 旧状态。
- 未验证项没有被写成 done。
- main 保持可复现、工作树干净。
- v1 标签不被移动或重写。

完成后停止继续开发，将交接报告和新的 closeout 提交 SHA 发回，由主 Codex 验收并接管 V1 交付主线。
```

## 6. 工程审查门

```text
Stage: products-plan-eng-review
Recommendation: go for tag validation and closeout
Gate: revise
Next: 完成 M72-M78 closeout、浏览器/账号证据和权威状态同步后，再启动 V1 新功能实现
```
